{% extends '@Config/layout.html.twig' %}
{% block body %}
    {#{% form_theme form '@User/Common/custom_errors.html.twig' %}#}
    <h2>{{ 'config' | trans }}</h2>
    <div class="container">
        {{ form_start(form, {'action': path('config_config_save'), 'method': 'POST'})  }}
        {{ form_widget(form) }}
        <button type="submit">{{ 'save'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}