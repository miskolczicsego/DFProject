{% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('bundles/home/css/starter.css') }}" />
{% endblock %}
<div class="container">
    {% if renderHistoryTable %}
        {% include('@Home/History/feedhistory.html.twig') %}
    {% else %}
        <h2 class="no-feeder">
            {{ 'noFeederMessage' | trans }}
        </h2>
        <span class="glyphicon glyphicon-plus-sign"></span>
        <a href="{{ path('feeder_feeder_add') }}">{{ 'linktoaddfeeder' | trans}}</a>
    {% endif %}
</div><!-- /.container -->